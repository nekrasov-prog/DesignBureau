@model IEnumerable<DesignBureauWebApplication.Models.Inventory>

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@*<h2>Инвентарь</h2>

<p>
    @Html.ActionLink("✚", "Create", new{}, new { @class="btn btn-primary" })
</p>
<table class="table table-hover table-responsive">
    <tr>
        <th>
            Наименование
        </th>
        <th>
            Тип
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.InventoryName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InventoryType)
            </td>
            <td>
                @Html.ActionLink("✏️", "Edit", new { id=item.InventoryId }, new { @class = "btn btn-primary" })
                @Html.ActionLink("🔍", "Details", new { id=item.InventoryId }, new { @class = "btn btn-success" })
                @Html.ActionLink("🗑️", "Delete", new { id=item.InventoryId }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    }

</table>*@