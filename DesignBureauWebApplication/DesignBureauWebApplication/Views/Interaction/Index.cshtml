@model IEnumerable<DesignBureauWebApplication.Models.Interaction>

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@*<h2>Взаимодействия</h2>

<p>
    @Html.ActionLink("✚", "Create", new{}, new { @class="btn btn-primary" })
</p>
<table class="table table-hover table-responsive">
    <tr>
        <th>
            Взаимодействие
        </th>
        <th>
            Время
        </th>
        <th>
            Статус
        </th>
        <th>
            Инвентарь
        </th>
        <th>
            Сотрудник
        </th>
        <th>
            Работа
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.InteractionDictionary.InteractionTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InteractionStatus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Inventory.InventoryName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employee.ELastName) @Html.DisplayFor(modelItem => item.Employee.EFirstName) @Html.DisplayFor(modelItem => item.Employee.EPatronymic)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Work.WorkDictionary.WorkTitle)
            </td>
            <td>
                @Html.ActionLink("✏️", "Edit", new { id=item.InteractionId }, new { @class = "btn btn-primary" })
                @Html.ActionLink("🔍", "Details", new { id=item.InteractionId }, new { @class = "btn btn-success" })
                @Html.ActionLink("🗑️", "Delete", new { id=item.InteractionId }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    }

</table>*@